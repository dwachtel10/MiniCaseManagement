@model IEnumerable<CaseManagementv4Code.Models.Charge>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ChargeName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Severity)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ORCSection)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CaseDetail.DefendantLastName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ChargeName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Severity)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ORCSection)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CaseDetail.DefendantLastName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ChargeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ChargeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ChargeID })
        </td>
    </tr>
}

</table>
